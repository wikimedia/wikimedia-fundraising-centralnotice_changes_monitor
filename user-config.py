""" Config file required by pywikibot, since programmatic config is not supported.
For developer setup, we manually override the URL for API calls. See
config_example.yaml.
"""
family = 'meta'
mylang = 'en'
usernames[ 'meta' ][ 'en' ] = 'PywikiBotDummyUser'